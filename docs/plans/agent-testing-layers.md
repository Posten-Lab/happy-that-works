# Agent Testing Layers

Keep this simple.

## Layer 1

Layer 1 is direct integration with the real agent.

Source of truth:

- `packages/talos-cli/agents.md`

## Layer 2

Layer 2 is full Talos product validation.

Source of truth:

- `./product.md`

## talos-agent spawn

`talos-agent spawn` is product-layer validation work.

Source of truth:

- `./talos-agent.md`
- `packages/talos-agent/src/talos-agent.integration.test.ts`
