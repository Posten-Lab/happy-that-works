# In-cluster Redis for talos-server (namespace `talos`).
#
# talos-server uses Redis for the socket.io pub/sub adapter + eventbus so that
# multiple server replicas stay in sync (main.ts / storage/redis.ts guard on
# REDIS_URL). Single instance, AOF-persisted on a local-path PVC. Reached via
# ClusterIP Service `talos-redis:6379`.
apiVersion: v1
kind: Service
metadata:
  name: talos-redis
  namespace: talos
  labels:
    app: talos-redis
spec:
  selector:
    app: talos-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: talos-redis-data
  namespace: talos
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: talos-redis
  namespace: talos
  labels:
    app: talos-redis
spec:
  replicas: 1
  # Recreate: a local-path PVC is bound to one node; never run two pods on it.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: talos-redis
  template:
    metadata:
      labels:
        app: talos-redis
    spec:
      nodeSelector:
        kubernetes.io/hostname: server-guru-eu
      containers:
        - name: redis
          image: redis:7-alpine
          args: ["redis-server", "--appendonly", "yes"]
          ports:
            - name: redis
              containerPort: 6379
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "20m", memory: "64Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: talos-redis-data
