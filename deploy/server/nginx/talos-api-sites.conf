# Edge nginx site for the self-hosted Talos relay API — ${TALOS_API_HOST}.
#
# Proxies to the talos_api NodePort upstream (talos-server). The mobile app +
# CLI open a socket.io WebSocket at /v1/updates, so this vhost is WebSocket-aware
# via the shared $connection_upgrade map (conf.d/00-websocket-map.conf).
#
# TLS from certbot on the edge VM:
#   sudo certbot certonly --nginx -d ${TALOS_API_HOST}
#
# Attachments do NOT flow through here — in S3 mode the client uploads/downloads
# bytes directly to ${TALOS_FILES_HOST} (MinIO) via presigned URLs. This
# vhost only carries the (small, encrypted-JSON) control API, so a modest body
# limit is fine.

server {
    server_name ${TALOS_API_HOST};
    access_log /var/log/nginx/access.log json_combined;

    client_max_body_size 25m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options "nosniff" always;

    location / {
        proxy_pass http://talos_api;
        proxy_http_version 1.1;

        # WebSocket upgrade (socket.io /v1/updates). $connection_upgrade is
        # defined in conf.d/00-websocket-map.conf.
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        # Long read timeout so idle WebSockets aren't culled mid-session.
        proxy_read_timeout 3600s;

        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries 3;
        proxy_next_upstream_timeout 15s;
    }

    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/${TALOS_API_HOST}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${TALOS_API_HOST}/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

# HTTP -> HTTPS redirect
server {
    listen 80;
    server_name ${TALOS_API_HOST};
    if ($host = ${TALOS_API_HOST}) { return 301 https://$host$request_uri; }
    return 404;
}
